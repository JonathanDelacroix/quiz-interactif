import { TTSAudioBuffer, TTSProviderProps } from '..';
import { Fetcher } from '../../utils/Fetcher';
export declare class GoogleTTS implements TTSProviderProps {
    private readonly fetcher;
    constructor({ fetcher }?: {
        fetcher?: Fetcher;
    });
    getAudioBuffer(text: string, language: string): Promise<TTSAudioBuffer>;
    static getSupportedLanguages(): string[];
}
